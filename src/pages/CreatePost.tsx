import React from 'react';

const CreatePostPage: React.FC = () => {
    return (
        <div>
            <h1>Create New Post</h1>
            {/* Form for creating a new post will be here */}
        </div>
    );
};

export default CreatePostPage;
